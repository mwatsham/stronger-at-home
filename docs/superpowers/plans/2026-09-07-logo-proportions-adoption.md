# Approved logo proportions adoption

The project sponsor approved the final review-profile layout and requested its
adoption throughout the project on 7 September 2026.

1. Preserve the v2 source artwork and transparent master as historical inputs.
2. Promote a v3 primary layout: complete three-line wordmark at 75% of the
   symbol's visible height, vertically centred; gap 39/469 of the symbol height.
3. Generate current opaque primary assets, transparent primary sizes and email
   artwork. Retain the approved square and wide review-profile images.
4. Synchronize the shared website logo, approval ledger, asset hashes, identity
   guidance and machine-readable context. Standalone marks are unaffected.
5. Verify exact source integrity, dimensions, height ratio, gap and centring;
   run the complete existing quality gate and review the website rendering.

Artwork is proportionally resampled from existing PNG pixels. There is no
redrawing, retyping, recolouring or SVG generation. Historical approval records
remain historical; D-41 is the current layout authority. Production publication
is outside this project-file change.
